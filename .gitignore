# Reference papers (kept locally, not version-controlled)
*.pdf

# Test data / dumps (multi-GB, kept locally)
/test-discs/

# Go build artefacts
/miniscram
/bin/
/dist/
*.exe
*.test
*.out

# Go workspace state
go.work.sum

# Nix build symlink (created by `nix build`)
/result

# Editor / OS noise
.DS_Store
.idea/
.vscode/

# Local worktrees (created by `git worktree add .worktrees/...`)
/.worktrees/
